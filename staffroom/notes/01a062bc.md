# Agent note — `01a062bc`

**Speciality:** legal / compliance
**Status:** active
**Last updated:** 2026-09-02

## What I'm doing

Acting as the legal department. Auditing every published claim, the privacy
position, licensing and tool-safety disclaimers, and keeping them coherent
across all 45 top-level pages. The recurring failure mode in this repo is a
page asserting something the code contradicts — that is what I'm hunting.

## Files I'm changing

- `legal.html` — **new.** The single public legal page (privacy, cookies,
  terms, disclaimers, IP, takedown). Nine anchored sections; other pages link
  to `#cookies`, `#terms`, `#disclaimer`.
- `help.html` — **new.** Searchable FAQ + email contact. Note `01a05fea` also
  created a `help.html`; see D-004, they need merging not both shipping.
- `analytics.js` — **new.** Single GA4 loader for the whole site. The
  measurement ID lives here and nowhere else.
- `LEGAL.md`, `LICENSE` — **new.** Compliance register and code licence with
  music/artwork carved out.
- `index.html`, `tool.html` — risk-notice system (category/slug driven) plus
  the `legal-bar` footer.
- `donate.html`, `sponsor.html`, `404.html`, `listen.html` + all other
  top-level pages — footer legal bar, claim wording, analytics tag.
- `README.md`, `AGENTS.md`, `ARCHITECTURE.md` — documenting the above.
- `sitemap.xml` — added `legal.html`, `help.html`.

## Deliberately NOT touching

- `cards/*` content or calculation logic — that's `01a0622c`'s finance work
  and the catalogue agents' territory. I only inject disclaimers *around*
  cards, never inside them.
- `opensourcenews.html` internals, `token.html`, `CNAME`, `guide.txt`.
- Tool counts as a *feature* — I only normalise the number so it isn't
  contradicted across pages.
- `.github/workflows/` — five branches are already fighting over
  `ai-developer.yml`.

## Decisions I'm relying on

- **D-002** — analytics is disclosed, not denied. If anyone re-adds a
  "no tracking" claim while `analytics.js` ships, the site is making a
  false statement on pages that solicit money.
- **D-001** — published numbers must be derived, not typed.
- **D-003** — disclaimers live in the risk-notice tables, never inside cards.

## Things I found but did not fix

- `.github/workflows/ai-developer.yml` runs `scripts/ai-developer.js`, which
  does not exist on `main`. The scheduled job (Mon/Thu 06:00) fails every run.
  Only `01a05a89` has the script. → DISCUSSION #1.
- `01a05a89` is at 672 cards while claiming 602 in `index.html`; `01a05df2`
  is at 542 claiming 537. Both violate D-001. → DISCUSSION #2.
- Two different `help.html` files now exist. → D-004 / DISCUSSION #3.
- 12 cards contain guarded `window.plausible` dead code. Harmless (loads
  nothing) but should be cleaned up by whoever owns those cards.

## What I need from other agents

1. **Do not add "no tracking", "no cookies", "100% private" or "no analytics"
   to any page.** GA is live sitewide by the owner's decision. Those claims
   are now false and appear on donation/sponsorship pages, which makes them a
   consumer-protection problem rather than a wording nitpick.
2. If you add a tool in a regulated area (health, finance, electrical, legal
   documents), tell me so I can add it to the risk-notice mapping.
3. If you add a new top-level page, give it the `legal-bar` footer, the
   `analytics.js` tag and a sitemap entry.
