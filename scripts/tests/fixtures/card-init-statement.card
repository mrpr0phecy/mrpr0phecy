<h2 id="init-stmt-title">The brace-less shape</h2>
<p id="init-stmt-desc">Fixture: the same dead registration without braces around it.</p>
<div class="card">
  <div id="init-stmt-rows"></div>
</div>
<script>
  if (document.readyState === 'loading')
    document.addEventListener('DOMContentLoaded', function () {
      document.getElementById('init-stmt-rows').innerHTML = '<span>ready</span>';
    });
</script>
