__pycache__/
*.pyc

# installed locally to run scripts/card-runtime-sweep.js (needs jsdom);
# CI installs it per-run, it is never committed
node_modules/

# local design preview screenshots (not part of the site)
.design-preview/

# AI Developer facility — drafts & reports are never committed
# (promoted by a human after review, per AGENTS.md §8)
ai-developer/
